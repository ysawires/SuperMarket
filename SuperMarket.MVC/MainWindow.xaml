<Window x:Class="SuperMarket.MVC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SuperMarket.MVC"
        FontSize="32"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel
        DataContext="{Binding RelativeSource={RelativeSource AncestorType=local:MainWindow}, Path=Grocery}"
        >
        <TextBlock Text="{Binding Path=ShoppingCart.Total}" />
        <TextBlock Text="{Binding Path=Count}"/>
        <ItemsControl ItemsSource="{Binding Products}"
                      ButtonBase.Click="Button_Click" />
        <ItemsControl ItemsSource="{Binding Path=ShoppingCart.Products}"
                      ItemTemplate="{StaticResource ReadonlyTemplate}" />
    </StackPanel>
</Window>
